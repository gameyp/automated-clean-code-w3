import automated_clean_code

def test_is_palindrome_true():
    assert automated_clean_code.is_palindrome("racecar") is True

def test_is_palindrome_false():
    assert automated_clean_code.is_palindrome("hello") is False
